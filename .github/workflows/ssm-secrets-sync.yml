# REMOVED: This workflow previously synced GitHub Secrets to AWS SSM Parameter Store.
# The project is now configured to deploy on Vercel; AWS-specific automation was removed to keep the repo minimal.
# If you need to sync secrets to SSM in the future, recreate the workflow or use the AWS console.